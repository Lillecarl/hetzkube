apiVersion: postgresql.cnpg.io/v1
ekn:
  gitOpsTarget: everything
kind: Database
metadata:
  name: keycloak
  namespace: database
spec:
  cluster:
    name: pg0
  databaseReclaimPolicy: delete
  name: keycloak
  owner: keycloak
