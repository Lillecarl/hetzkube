apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: admin
  namespace: keycloak
spec:
  data:
  - remoteRef:
      key: name:keycloak-admin
      property: username
    secretKey: username
  - remoteRef:
      key: name:keycloak-admin
      property: password
    secretKey: password
  refreshInterval: 30m0s
  secretStoreRef:
    kind: ClusterSecretStore
    name: scaleway
  target:
    template:
      type: kubernetes.io/basic-auth
